# OS files
Thumbs.db
.DS_Store

# Browser cache / temporary files
AppData/
*.tmp
*.cache

# Environment variables (if we use .env later)
.env

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
